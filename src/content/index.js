// Content map for bundled production builds
// In development mode, ContentService will fetch files directly from filesystem
// This ensures the import doesn't fail

const contentMap = {
  // This will be populated by the build process for production
  // In development, files are fetched dynamically
};

export default contentMap; 